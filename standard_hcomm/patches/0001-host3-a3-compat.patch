From 33d156bf832744a4f767144149ed1b2e304ff2e3 Mon Sep 17 00:00:00 2001
From: zhaojunjie_ <zhaojunjie8@huawei.com>
Date: Mon, 21 Sep 2026 20:19:37 +0800
Subject: [PATCH] Fix A3 resource-channel interoperability with Host RoCE
 plugin

---
 .../channel/host_cpu_roce_channel.cc          | 51 ++++++++++-
 .../base_comm/channel/host_cpu_roce_channel.h |  4 +-
 .../base_comm/endpoint/cpu_roce_endpoint.cc   |  9 ++
 .../channels/aicpu/aicpu_ts_roce_channel.cc   | 18 ++++
 .../resources/reged_mems/aicpu_ts_roce_mem.cc | 38 +++++++-
 test/ut/framework/next/CMakeLists.txt         |  1 +
 .../reged_mems/ut_aicpu_host_roce_import.cc   | 91 +++++++++++++++++++
 7 files changed, 207 insertions(+), 5 deletions(-)
 create mode 100644 test/ut/framework/next/comms/endpoints/reged_mems/ut_aicpu_host_roce_import.cc

diff --git a/experimental/base_comm/channel/host_cpu_roce_channel.cc b/experimental/base_comm/channel/host_cpu_roce_channel.cc
index c46a785e6..1aed74d46 100644
--- a/experimental/base_comm/channel/host_cpu_roce_channel.cc
+++ b/experimental/base_comm/channel/host_cpu_roce_channel.cc
@@ -185,7 +185,9 @@ HcclResult HostCpuRoceChannel::BuildSocket()
         port = DEFAULT_LISTENING_PORT;
         HCCL_INFO("[HostCpuRoceChannel::%s] channelDesc port is 0, use default port [%u]", __func__, port);
     }
-    std::string socketTag = "AUTOMATIC_SOCKET_TAG";
+    // Match the public channelName just like the built-in Host RoCE channel.
+    std::string socketTag = channelDesc_.channelName != nullptr
+        ? std::string(channelDesc_.channelName) : "AUTOMATIC_SOCKET_TAG";
     Hccl::SocketConfig socketConfig = (channelDesc_.role != HCOMM_SOCKET_ROLE_RESERVED)
         ? Hccl::SocketConfig(linkData, port, socketTag, channelDesc_.role == HCOMM_SOCKET_ROLE_SERVER)
         : Hccl::SocketConfig(linkData, port, socketTag);
@@ -1552,6 +1554,16 @@ HcclResult HostCpuRoceChannel::RegisterUserMemHybird()
 
 HcclResult HostCpuRoceChannel::BuildExchangeDataLengthHybird()
 {
+    if (channelDesc_.exchangeAllMems) {
+        // A2/A3 resource channels use userMemEnable=false, drainEnable=true,
+        // isIndOp=true. Their wire format differs from the collective channel:
+        // qp count | drain destination | drain source | atomic flag | two counts.
+        // Application memory has already been exported/imported via HcommMem*.
+        CHK_PRT_RET(connections_.size() != 1 || notifyNum_ != 0,
+            HCCL_ERROR("Hybrid resource channel supports one QP and zero user notifies"), HCCL_E_NOT_SUPPORT);
+        exchangeDataTotalSize_ = sizeof(u32) * 3 + sizeof(hccl::MemMsg) * 2 + sizeof(u8);
+        return HCCL_SUCCESS;
+    }
     exchangeDataTotalSize_ = 0;
     exchangeDataTotalSize_ += sizeof(u32); // qp数量
     exchangeDataTotalSize_ += sizeof(hccl::MemMsg) * 2; // output、input buffer
@@ -1575,6 +1587,19 @@ HcclResult HostCpuRoceChannel::BuildExchangeDataHybird()
     data += sizeof(u32);
     size -= sizeof (u32);
 
+    if (channelDesc_.exchangeAllMems) {
+        CHK_RET(CreateNotifyBufferHybird(hccl::DATA_NOTIFY_MEM, 1, data, size));
+        CHK_RET(CreateNotifyValueBufferHybird());
+        CHK_SAFETY_FUNC_RET(memcpy_s(data, size, &localMemMsg_[hccl::NOTIFY_SRC_MEM], sizeof(hccl::MemMsg)));
+        data += sizeof(hccl::MemMsg);
+        size -= sizeof(hccl::MemMsg);
+        // resize value-initializes the tail to zero: no atomic-write capability,
+        // no additional device/host MR records. READ uses the imported real rkey.
+        CHK_PRT_RET(size != sizeof(u8) + 2 * sizeof(u32),
+            HCCL_ERROR("Invalid hybrid resource exchange tail"), HCCL_E_INTERNAL);
+        return HCCL_SUCCESS;
+    }
+
     CHK_SAFETY_FUNC_RET(memcpy_s(data, size, reinterpret_cast<void *>(&localMemMsg_[hccl::USER_OUTPUT_MEM]), sizeof(hccl::MemMsg)));
     data += sizeof(hccl::MemMsg);
     size -= sizeof (hccl::MemMsg);
@@ -1610,6 +1635,8 @@ HcclResult HostCpuRoceChannel::GetRemoteAddrHybird(hccl::MemType memType, u8 *&d
 
 HcclResult HostCpuRoceChannel::ParseRecvExchangeDataHybird()
 {
+    CHK_PRT_RET(exchangeDataForRecv_.size() != exchangeDataTotalSize_ || exchangeDataTotalSize_ < sizeof(u32),
+        HCCL_ERROR("Truncated hybrid exchange data"), HCCL_E_PARA);
     u8 *data = exchangeDataForRecv_.data();
     u64 size = exchangeDataTotalSize_;
 
@@ -1618,6 +1645,24 @@ HcclResult HostCpuRoceChannel::ParseRecvExchangeDataHybird()
     data += sizeof(u32);
     size -= sizeof(u32);
 
+    if (channelDesc_.exchangeAllMems) {
+        CHK_PRT_RET(remoteQpNum != 1, HCCL_ERROR("Invalid hybrid resource QP count"), HCCL_E_PARA);
+        CHK_RET(GetRemoteAddrHybird(hccl::DATA_NOTIFY_MEM, data, size));
+        CHK_RET(GetRemoteAddrHybird(hccl::NOTIFY_SRC_MEM, data, size));
+        CHK_PRT_RET(size != sizeof(u8) + 2 * sizeof(u32),
+            HCCL_ERROR("Invalid hybrid resource exchange tail"), HCCL_E_PARA);
+        // The resource-channel transport exchanges zero extra MR records; its
+        // application MRs are managed separately by HcommMemExport/Import.
+        u32 deviceCount = 0;
+        u32 hostCount = 0;
+        CHK_SAFETY_FUNC_RET(memcpy_s(&deviceCount, sizeof(deviceCount), data + sizeof(u8), sizeof(deviceCount)));
+        CHK_SAFETY_FUNC_RET(memcpy_s(&hostCount, sizeof(hostCount),
+            data + sizeof(u8) + sizeof(u32), sizeof(hostCount)));
+        CHK_PRT_RET(deviceCount != 0 || hostCount != 0,
+            HCCL_ERROR("Unexpected hybrid resource MR records"), HCCL_E_NOT_SUPPORT);
+        return HCCL_SUCCESS;
+    }
+
     CHK_RET(GetRemoteAddrHybird(hccl::USER_OUTPUT_MEM, data, size));
     CHK_RET(GetRemoteAddrHybird(hccl::USER_INPUT_MEM, data, size));
     CHK_RET(GetRemoteAddrHybird(hccl::DATA_NOTIFY_MEM, data, size));
@@ -1684,7 +1729,9 @@ HcclResult HostCpuRoceChannel::ExchangeDataHybird()
 {
     HCCL_INFO("[Hybrid] Starting hybrid data exchange");
 
-    CHK_RET(RegisterUserMemHybird());
+    if (!channelDesc_.exchangeAllMems) {
+        CHK_RET(RegisterUserMemHybird());
+    }
 
     CHK_RET(BuildExchangeDataHybird());
 
diff --git a/experimental/base_comm/channel/host_cpu_roce_channel.h b/experimental/base_comm/channel/host_cpu_roce_channel.h
index 70212d4e9..634bd4247 100644
--- a/experimental/base_comm/channel/host_cpu_roce_channel.h
+++ b/experimental/base_comm/channel/host_cpu_roce_channel.h
@@ -177,8 +177,8 @@ private:
     uint32_t localNotifySize_;
     uint32_t localNotifyAccess_;
 
-    std::array<hccl::MemMsg, static_cast<u32>(hccl::MemType::MEM_TYPE_RESERVED)> localMemMsg_;
-    std::array<hccl::MemMsg, static_cast<u32>(hccl::MemType::MEM_TYPE_RESERVED)> remoteMemMsg_;
+    std::array<hccl::MemMsg, static_cast<u32>(hccl::MemType::MEM_TYPE_RESERVED)> localMemMsg_{};
+    std::array<hccl::MemMsg, static_cast<u32>(hccl::MemType::MEM_TYPE_RESERVED)> remoteMemMsg_{};
     uint64_t exchangeDataTotalSize_;
     std::vector<uint8_t> exchangeDataForSend_;
     std::vector<uint8_t> exchangeDataForRecv_;
diff --git a/experimental/base_comm/endpoint/cpu_roce_endpoint.cc b/experimental/base_comm/endpoint/cpu_roce_endpoint.cc
index 6c38e6b96..b26409261 100644
--- a/experimental/base_comm/endpoint/cpu_roce_endpoint.cc
+++ b/experimental/base_comm/endpoint/cpu_roce_endpoint.cc
@@ -20,6 +20,7 @@
 #include "orion_adapter_hccp.h"
 #include "server_socket_manager.h"
 #include "hccp.h"
+#include "dlra_function.h"
 
 using Hccl::HcclException;
 using std::string;
@@ -54,6 +55,14 @@ HcclResult CpuRoceEndpoint::Init()
     }
     Hccl::IpAddress ipAddr{};
     CHK_RET(hcomm::CommAddrToIpAddress(endpointDesc_.commAddr, ipAddr));
+    // Hybrid channels use A2/A3 RA adapters as well as the Host PEER adapters.
+    // Host-only endpoints do not enter GlobalNetDevMgr, so initialize the
+    // RA symbol table here before any hybrid MR/QP operation can use it.
+    CHK_RET(hccl::DlRaFunction::GetInstance().DlRaFunctionInit());
+    // Match CpuUrmaEndpoint and the device HCCP handshake: the peer validates
+    // its IP/tag and waits for the whitelist ACK before exchanging channel data.
+    // Set this before PEER initialization, which snapshots the switch value.
+    TRY_CATCH_RETURN(Hccl::HrtRaSocketSetWhiteListStatus(1));
     CHK_RET(InitHostPeerRaOnce(kHostResourceId, "CpuRoceEndpoint"));
     auto &rdmaHandleMgr = Hccl::RdmaHandleManager::GetInstance();
     TRY_CATCH_RETURN(ctxHandle_ = static_cast<void *>(
diff --git a/src/base_comm/resources/endpoint_pairs/channels/aicpu/aicpu_ts_roce_channel.cc b/src/base_comm/resources/endpoint_pairs/channels/aicpu/aicpu_ts_roce_channel.cc
index 04b96da58..4a697c9ac 100644
--- a/src/base_comm/resources/endpoint_pairs/channels/aicpu/aicpu_ts_roce_channel.cc
+++ b/src/base_comm/resources/endpoint_pairs/channels/aicpu/aicpu_ts_roce_channel.cc
@@ -113,6 +113,24 @@ HcclResult WaitClientSocketLinkEstablished(const std::shared_ptr<hccl::HcclSocke
 
 HcclResult AicpuTsRoceChannel::BuildSocketTagName(std::string &outTag) const
 {
+    if (remoteEp_.loc.locType == ENDPOINT_LOC_TYPE_HOST) {
+        // Host SocketConfig uses <channelName>_<server IP>_<client IP>_<port>.
+        // Preserve the existing device-device tag convention below, but use the
+        // Host convention for this mixed pair so RA can match the same socket.
+        hccl::HcclIpAddress localIp{};
+        hccl::HcclIpAddress remoteIp{};
+        CHK_RET(CommAddrToHcclIp(localEp_.commAddr, localIp));
+        CHK_RET(CommAddrToHcclIp(remoteEp_.commAddr, remoteIp));
+        const std::string serverIp(isLocalIpClient_ ? remoteIp.GetReadableIP() : localIp.GetReadableIP());
+        const std::string clientIp(isLocalIpClient_ ? localIp.GetReadableIP() : remoteIp.GetReadableIP());
+        const uint32_t port = channelDesc_.port != 0 ? channelDesc_.port : kDefaultRocePort;
+        outTag = (channelDesc_.channelName != nullptr ? std::string(channelDesc_.channelName)
+                                                     : "AUTOMATIC_SOCKET_TAG")
+            + "_" + serverIp + "_" + clientIp + "_" + std::to_string(port);
+        CHK_PRT_RET(outTag.size() + 1U > SOCK_CONN_TAG_SIZE,
+            HCCL_ERROR("[AicpuTsRoceChannel] Host socket tag too long"), HCCL_E_PARA);
+        return HCCL_SUCCESS;
+    }
     if (channelDesc_.channelName != nullptr) {
         outTag = std::string(channelDesc_.channelName);
         if (outTag.size() + 1U > SOCK_CONN_TAG_SIZE) {
diff --git a/src/base_comm/resources/reged_mems/aicpu_ts_roce_mem.cc b/src/base_comm/resources/reged_mems/aicpu_ts_roce_mem.cc
index b63eab299..3c915099f 100644
--- a/src/base_comm/resources/reged_mems/aicpu_ts_roce_mem.cc
+++ b/src/base_comm/resources/reged_mems/aicpu_ts_roce_mem.cc
@@ -16,6 +16,7 @@
 #include "hccl_network.h"
 #include "log.h"
 #include "rma_buffer.h"
+#include "exchange_rdma_buffer_dto.h"
 
 namespace {
 using LocalRdmaRmaBufferMgr = hccl::NetDevContext::LocalRdmaRmaBufferMgr;
@@ -244,6 +245,40 @@ HcclResult AicpuTsRoceRegedMemMgr::GetParamsFromMemDesc(const void *memDesc, uin
         return HCCL_E_INTERNAL;
     }
     rdmaBlob.assign(base, base + descLen - sizeof(EndpointDesc));
+    if (endpointDesc.loc.locType == ENDPOINT_LOC_TYPE_HOST && endpointDesc.protocol == COMM_PROTOCOL_ROCE) {
+        // Host RoCE plugin exports ExchangeRdmaBufferDto, whereas A2/A3 exports
+        // LocalRdmaRmaBuffer's wire format. Translate inside hcomm so applications
+        // can continue passing opaque HcommMemExport/Import descriptors unchanged.
+        constexpr size_t fixedBytes = sizeof(u64) * 2 + sizeof(u32) + sizeof(size_t);
+        CHK_PRT_RET(rdmaBlob.size() < fixedBytes,
+            HCCL_ERROR("[GetParamsFromMemDesc] truncated Host RoCE descriptor"), HCCL_E_PARA);
+        size_t tagBytes = 0;
+        CHK_SAFETY_FUNC_RET(memcpy_s(&tagBytes, sizeof(tagBytes),
+            rdmaBlob.data() + sizeof(u64) * 2 + sizeof(u32), sizeof(tagBytes)));
+        // BinaryStream does not bound string allocations: validate before parsing.
+        CHK_PRT_RET(tagBytes != rdmaBlob.size() - fixedBytes,
+            HCCL_ERROR("[GetParamsFromMemDesc] invalid Host RoCE tag length"), HCCL_E_PARA);
+        std::vector<char> wire(rdmaBlob.begin(), rdmaBlob.end());
+        Hccl::BinaryStream stream(wire);
+        Hccl::ExchangeRdmaBufferDto dto;
+        dto.Deserialize(stream);
+        CHK_PRT_RET(dto.addr == 0 || dto.size == 0 || dto.size > UINT64_MAX - dto.addr,
+            HCCL_ERROR("[GetParamsFromMemDesc] invalid Host RoCE address range"), HCCL_E_PARA);
+
+        const u8 type = static_cast<u8>(hccl::RmaType::RDMA_RMA);
+        const auto address = reinterpret_cast<void *>(static_cast<uintptr_t>(dto.addr));
+        const auto memType = hccl::RmaMemType::HOST;
+        std::ostringstream translated;
+        translated.write(reinterpret_cast<const char *>(&type), sizeof(type));
+        translated.write(reinterpret_cast<const char *>(&address), sizeof(address));
+        translated.write(reinterpret_cast<const char *>(&dto.size), sizeof(dto.size));
+        // Remote Host memory is addressed by its exported VA. This is an RDMA
+        // remote address, not a local mapping into NPU HBM.
+        translated.write(reinterpret_cast<const char *>(&address), sizeof(address));
+        translated.write(reinterpret_cast<const char *>(&memType), sizeof(memType));
+        translated.write(reinterpret_cast<const char *>(&dto.rkey), sizeof(dto.rkey));
+        rdmaBlob = translated.str();
+    }
     return HCCL_SUCCESS;
 }
 
@@ -278,7 +313,8 @@ HcclResult AicpuTsRoceRegedMemMgr::MemoryImport(const void *memDesc, uint32_t de
 
     outMem->addr = remoteBuf->GetAddr();
     outMem->size = remoteBuf->GetSize();
-    outMem->type = COMM_MEM_TYPE_DEVICE;
+    outMem->type = remoteBuf->GetMemType() == hccl::RmaMemType::HOST
+        ? COMM_MEM_TYPE_HOST : COMM_MEM_TYPE_DEVICE;
     HCCL_INFO("[AicpuTsRoceRegedMemMgr][MemoryImport] success descLen[%u] outMem addr[%p] size[%llu], mgrCnt[%zu]",
         descLen, outMem->addr, static_cast<unsigned long long>(outMem->size), remoteRdmaRmaBufferMgrs_.size());
     return HCCL_SUCCESS;
diff --git a/test/ut/framework/next/CMakeLists.txt b/test/ut/framework/next/CMakeLists.txt
index d15f4fe97..87ee8c67e 100644
--- a/test/ut/framework/next/CMakeLists.txt
+++ b/test/ut/framework/next/CMakeLists.txt
@@ -62,6 +62,7 @@ set(src_to_test_list
     comms/endpoints/reged_mems/ut_hccs_reged_mem_mgr_test.cc
     coll_comms/api_c_adpt/ut_coll_comm_res_c_adpt.cc
     comms/endpoints/reged_mems/ut_aicpu_ts_roce_mem.cc
+    comms/endpoints/reged_mems/ut_aicpu_host_roce_import.cc
     # Ns recovery相关测试用例
     comms/endpoint_pairs/channels/aicpu/ut_aicpu_ts_urma_channel.cc
     comms/endpoint_pairs/channels/aicpu/ut_aicpu_ts_p2p_channel.cc
diff --git a/test/ut/framework/next/comms/endpoints/reged_mems/ut_aicpu_host_roce_import.cc b/test/ut/framework/next/comms/endpoints/reged_mems/ut_aicpu_host_roce_import.cc
new file mode 100644
index 000000000..2c9c105f1
--- /dev/null
+++ b/test/ut/framework/next/comms/endpoints/reged_mems/ut_aicpu_host_roce_import.cc
@@ -0,0 +1,91 @@
+/**
+ * Copyright (c) 2026 Huawei Technologies Co., Ltd.
+ * This program is free software, you can redistribute it and/or modify it under the terms and conditions of
+ * CANN Open Software License Agreement Version 2.0 (the "License").
+ * Please refer to the License for details. You may not use this file except in compliance with the License.
+ * THIS SOFTWARE IS PROVIDED ON AN "AS IS" BASIS, WITHOUT WARRANTIES OF ANY KIND, EITHER EXPRESS OR IMPLIED,
+ * INCLUDING BUT NOT LIMITED TO NON-INFRINGEMENT, MERCHANTABILITY, OR FITNESS FOR A PARTICULAR PURPOSE.
+ * See LICENSE in the root of the software repository for the full text of the License.
+ */
+
+#include <gtest/gtest.h>
+#include <arpa/inet.h>
+#include <securec.h>
+#include "exchange_rdma_buffer_dto.h"
+#define private public
+#include "aicpu_ts_roce_mem.h"
+#include "aicpu_ts_roce_channel.h"
+#undef private
+using namespace hcomm;
+
+namespace {
+std::vector<char> HostRoceImportDescriptor()
+{
+    Hccl::ExchangeRdmaBufferDto dto(0x100000, 4096, 0x1234, "host-dram");
+    Hccl::BinaryStream stream;
+    dto.Serialize(stream);
+    std::vector<char> result;
+    stream.Dump(result);
+    EndpointDesc ep{};
+    EndpointDescInit(&ep, 1);
+    ep.protocol = COMM_PROTOCOL_ROCE;
+    ep.loc.locType = ENDPOINT_LOC_TYPE_HOST;
+    const auto *tail = reinterpret_cast<const char *>(&ep);
+    result.insert(result.end(), tail, tail + sizeof(ep));
+    return result;
+}
+}
+
+TEST(AicpuHostRoceImport, PreservesHostAddressSizeTypeAndKey)
+{
+    AicpuTsRoceRegedMemMgr mgr(nullptr, nullptr);
+    auto descriptor = HostRoceImportDescriptor();
+    HcommMem mem{};
+    ASSERT_EQ(mgr.MemoryImport(descriptor.data(), descriptor.size(), &mem), HCCL_SUCCESS);
+    EXPECT_EQ(mem.addr, reinterpret_cast<void *>(0x100000));
+    EXPECT_EQ(mem.size, 4096U);
+    EXPECT_EQ(mem.type, COMM_MEM_TYPE_HOST);
+    std::vector<RoceMemDetails> remote;
+    mgr.GatherRemoteMemDetails(remote);
+    ASSERT_EQ(remote.size(), 1U);
+    EXPECT_EQ(remote[0].key, 0x1234U);
+    EXPECT_EQ(remote[0].devAddr, 0x100000U);
+    EXPECT_EQ(mgr.MemoryUnimport(descriptor.data(), descriptor.size()), HCCL_SUCCESS);
+}
+
+TEST(AicpuHostRoceImport, RejectsTruncatedAndOversizedTagBeforeAllocation)
+{
+    AicpuTsRoceRegedMemMgr mgr(nullptr, nullptr);
+    auto descriptor = HostRoceImportDescriptor();
+    HcommMem mem{};
+    const size_t invalidSize = SIZE_MAX;
+    ASSERT_EQ(memcpy_s(descriptor.data() + sizeof(u64) * 2 + sizeof(u32), sizeof(size_t),
+        &invalidSize, sizeof(invalidSize)), EOK);
+    EXPECT_EQ(mgr.MemoryImport(descriptor.data(), descriptor.size(), &mem), HCCL_E_PARA);
+    descriptor.erase(descriptor.begin(), descriptor.begin() + 25);
+    EXPECT_EQ(mgr.MemoryImport(descriptor.data(), descriptor.size(), &mem), HCCL_E_PARA);
+}
+
+TEST(AicpuHostRoceTag, MatchesHostSocketConfigForBothRolesAndPreservesDeviceTag)
+{
+    HcommChannelDesc desc{};
+    desc.channelName = "read-demo";
+    desc.port = 19517;
+    AicpuTsRoceChannel channel(nullptr, desc);
+    channel.localEp_.commAddr.type = COMM_ADDR_TYPE_IP_V4;
+    channel.remoteEp_.commAddr.type = COMM_ADDR_TYPE_IP_V4;
+    channel.remoteEp_.loc.locType = ENDPOINT_LOC_TYPE_HOST;
+    ASSERT_EQ(inet_pton(AF_INET, "20.168.0.3", &channel.localEp_.commAddr.addr), 1);
+    ASSERT_EQ(inet_pton(AF_INET, "20.168.0.19", &channel.remoteEp_.commAddr.addr), 1);
+    std::string tag;
+    channel.isLocalIpClient_ = true;
+    ASSERT_EQ(channel.BuildSocketTagName(tag), HCCL_SUCCESS);
+    EXPECT_EQ(tag, "read-demo_20.168.0.19_20.168.0.3_19517");
+    channel.isLocalIpClient_ = false;
+    ASSERT_EQ(channel.BuildSocketTagName(tag), HCCL_SUCCESS);
+    EXPECT_EQ(tag, "read-demo_20.168.0.3_20.168.0.19_19517");
+    channel.remoteEp_.loc.locType = ENDPOINT_LOC_TYPE_DEVICE;
+    ASSERT_EQ(channel.BuildSocketTagName(tag), HCCL_SUCCESS);
+    EXPECT_EQ(tag, "read-demo");
+}
+
-- 
2.54.0.windows.1

